import React from "react";

const HeaderBanner = ({ headTitle }) => {
  return (
    <div className="head-banner">
      <div className="head-banner__title">{headTitle}</div>
    </div>
  );
};

export default HeaderBanner;
